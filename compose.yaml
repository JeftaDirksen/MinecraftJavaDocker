services:
  minecraftjava:
    build: .
    container_name: minecraftjava
    ports:
      - "25565:25565"
    volumes:
      - ./server:/data/server
    environment:
      - MEMORY=4G
      - EULA=true
      - SEED=abc
      - NVG_URL=${NVG_URL}
      - NVG_SECRET=${NVG_SECRET}
    restart: unless-stopped
    tty: true
    stdin_open: true
